# ══════════════════════════════════════════════════════════
# HAL Answering Service — Configuration
# ══════════════════════════════════════════════════════════
# Quick start: just run `python main.py` — HAL will walk you through setup.
#
# HAL starts in DEMO MODE (browser mic) until all production fields are
# filled in.  No flags needed — just fill in the blanks and restart.
#
# Lines starting with # are comments / optional with sane defaults.

# --- Owner ---
# Name used in greetings: "You've reached <name>'s phone."
OWNER_NAME=YourName

# --- LLM (Language Model) ---
# Provider: auto (inferred from URL), lmstudio, ollama, openai_compatible
LLM_PROVIDER=auto
# LM Studio default: http://127.0.0.1:1234/v1
# Ollama default:    http://127.0.0.1:11434/v1
LLM_BASE_URL=http://127.0.0.1:1234/v1
LLM_API_KEY=lm-studio
# LLM_MODEL=                # blank = server default (LM Studio) or set e.g. qwen3:4b (Ollama)
# LLM_MAX_TOKENS=200        # max response tokens
# LLM_TEMPERATURE=0.7       # 0.0-2.0
# LLM_FREQUENCY_PENALTY=0.0 # -2.0 to 2.0

# --- SignalWire (required for live phone calls) ---
# Get credentials at: https://signalwire.com
# Leave blank/commented to stay in demo mode.
# SIGNALWIRE_PROJECT_ID=    # your project UUID
# SIGNALWIRE_TOKEN=         # your API token
# SIGNALWIRE_SPACE=         # your space name
# SIGNALWIRE_PHONE_NUMBER=  # e.g. +14155551234
# SIGNALWIRE_SIGNING_KEY=   # blank = falls back to SIGNALWIRE_TOKEN

# --- Server ---
HOST=127.0.0.1
PORT=8080
# PUBLIC_HOST=              # e.g. caller.example.com (no https://)
# NO_TLS=                   # set to 1 if behind a reverse proxy / tunnel

# --- Voice / TTS (Chatterbox) ---
# Device: auto, cuda, or cpu
TTS_DEVICE=auto
# TTS_VOICE_PROMPT=hal9000.wav  # WAV file (>5s) for voice cloning
# TTS_MODEL_DIR=            # local Chatterbox weights (skips HF download)
# HF_TOKEN=                 # Hugging Face token if anonymous download fails

# --- Speech-to-Text / Faster-Whisper ---
# STT_MODEL=large-v3-turbo  # model size (tiny/base/small/medium/large-v3-turbo)
STT_DEVICE=auto
STT_COMPUTE_TYPE=auto
# STT_LANGUAGE=en           # blank = auto-detect
# STT_BEAM_SIZE=1
# STT_BEST_OF=1
# STT_NO_SPEECH_THRESHOLD=0.6
# STT_LOG_PROB_THRESHOLD=-1.0
# STT_CONDITION_ON_PREVIOUS_TEXT=false
# STT_INITIAL_PROMPT=Phone call screening conversation.

# --- Voice Activity Detection (Silero) ---
# VAD_SPEECH_THRESHOLD=0.5
# VAD_SILENCE_THRESHOLD_MS=400
# VAD_MIN_SPEECH_MS=250

# --- Security ---
# MAX_CONCURRENT_CALLS=3    # simultaneous calls
# MAX_CALL_DURATION_S=600   # 10-minute max per call

# --- Recording & Metadata ---
# RECORDINGS_DIR=recordings
# METADATA_DIR=metadata

# --- Push Notifications (ntfy.sh, optional) ---
# NTFY_TOPIC=               # create a topic at https://ntfy.sh
# NTFY_TOKEN=               # auth token for private topics
